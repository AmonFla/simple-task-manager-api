package daoConnection

/*import (
	"database/sql"

	_ "github.com/mattn/go-sqlite3"
)

func getSqliteInstance(File string) *sql.DB {
	if SingleInstance == nil {
		Once.Do(
			func() {
				var err error
				SingleInstance, err = sql.Open("sqlite3", File)
				if err != nil {
					panic(err)
				}
			})
	}
	return SingleInstance
}*/
